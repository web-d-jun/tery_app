import 'package:flutter/material.dart';

class LikedView extends StatefulWidget {
  _LikedView createState() => _LikedView();
}

class _LikedView extends State<LikedView> {
  Widget build(BuildContext context) {
    return Container(
      child: Text('like view'),
    );
  }
}
